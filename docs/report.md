# 账户行动报告契约

当前入口使用 `report/templates/account_report.html`，页面标题为“账户行动报告”。布局为3个锚点，移动端单列：

1. 账户总览：真实账户总资产、已核验可用现金、当前持仓数量→4只目标、过渡持仓数量、账户方向与三项待办、成长价值组内进度。
2. 现有持仓管理：优先显示非目标真实持仓的持有、反弹分批卖出、条件回补与最终盈利退出计划；页面成本参考和清仓回本价分列，不能只有等待清仓。
3. 目标组合与资金承接：按510500、159141、159259、159263固定角色排序，显示最终方向、已有目标持仓操作及价格/可用资金双条件。无即时配置或清完全部旧仓的要求。

不再有自选池/T网格页面或结果，也不把趋势榜、盈亏榜、风险榜拆成多个主导航。旧决策展示辅助函数已移除；历史模板不属于当前渲染入口。

持仓为0或空缺时不伪造持仓；实际可用现金未采得不能由“资产−市值”推断。组内40/60明确不是全账户配置。组内空仓不计算百分比；预算未定不显示建仓完成百分比。

价格显示为参考价，候选数量显示为「建议份额」，卡片保留具体执行限制，不逐项重复「待核验」。不宣称已下单、已改真实网格或一定触发。退出利润不能直接作为买入可用资金。AI不出现在首页制造另一套操作口径。

资金说明遵循 `LEGACY_RECOVERY_FIRST`：现有资金先用于待退出 ETF 的回本管理，目标组合仅使用扣除旧仓回补和周转预留后确认剩余的可用资金。报告应区别现金余额和实际可用资金；数据缺口逐项指出来源与影响，不将未持仓、已停用或非法数量混写为「待核验」。

正式报告必须通过数据质量与当前分析契约。测试预览必须标明无实时账户数据，不发布为正式建议。

本地测试预览run-id：TEST-role-policy-preview。HTML结构及自动化渲染测试已检查；浏览器URL安全策略拒绝打开file://预览，未完成截图验收。

数据折叠区保留各时间范围的已成交笔数、买入及卖出金额；卖出回款不标为盈利，也不据此断言本轮本金已回收。

用户纠正后：主导航顺序调整为账户总览→现有持仓管理→目标组合与资金承接。现有待退出持仓使用完整行动卡，列反弹卖价/数量、条件回补、清仓回本价及其不同核验边界。不得只展示“停买等清仓”。

实时批次 `20260907-175957` 已完成 Chrome 页面检查：3 个主章节、15 张行动卡，1920 像素桌面视口无页面横向溢出，首页截图已核对。截图保存在该批次的 `data/reports/` 中，不提交 Git。本次未进行移动端截图验收。

## 空值与建议文案（2026-09-08）

通用数值空值显示「—」。无持仓不显示缺失的浮盈亏，成长／价值都未建仓时不显示空百分比。未设置最小仓或最大仓时直接省略对应项；两项都未设时整行隐藏。显式0保留，策略建议另列。

双向参数每侧用一行展示「触发% → 确认% · 建议份额 · 首笔≈¥」：买 `−3.28% → +0.35%`，卖 `+2.81% → −0.23%`。不再单列单位与符号说明，卡片用「持1000」「¥7.771」「盈亏−3.03%」。资金未核实和净投入缺口仍明确说明，不用符号掩盖限制。

参数草案读取 `parameter_plan`，始终具有两侧比例、反弹/回落和≥100的合法数量；卡片显示行动阶段与标的专属限制，整单状态保留在结构化分析中。不得将空动作候选渲染为0份或单侧暂停。受限时草案不能直接用于运行网格；仅有买入条件但无库存时，同样暂不启用整单。无有效行情不展示残缺参数表。依据、数量来源、异常原单证据与详细核验折叠；卡片正文保留必要执行限制。

本次本地预览 run-id 为 `TEST-optional-grid-limits`，不含实时账户数据；旧发布报告需下一次正式采集、分析与发布后更新，不能把测试预览作为真实报告。

本次验证：全量 pytest 104 项通过，`compileall` 与 `git diff --check` 通过；本地预览覆盖5张行动卡及可选限制、空仓、无效单笔份额等文案。未重新采集或发布真实账户报告，未完成本次浏览器截图验收。

## 双向契约迁移与本地预览

本次分析/AI契约更新为 `etf_account_transition_v5`，拒绝旧单侧暂停建议直接重渲染。新版质量闸门校验六项参数和整单状态；历史账户及行情只用于标注数据时间的开发预览，不复用旧AI结论，不发布为实时建议。

本次开发对照预览：`data/reports/DEV-grid-platform-v5-preview.html`，复用批次 `20260908-084148` 的历史原始账户及行情重新计算规则，未重新采集、未复用旧AI、未发布。共15张行动卡及15组双向参数。使用本地离线 Chromium 核对1440桌面及390移动视口截图；页面无横向溢出，移动端参数表无需横向滚动。截图为同目录 `DEV-grid-platform-v5-desktop.png`、`DEV-grid-platform-v5-mobile.png`，均不提交Git。

验证：全量pytest 141项通过，compileall、Skill quick_validate与git diff --check通过。覆盖单笔卖1/持1000、零与异常数量、合法六项参数、小仓位、风险与上限联动、未设限制隐藏、显式0保留、v4和仅改版本迁移阻断。测试及历史预览不代表当前账户采集或真实委托验证。

## 综合依据与去重（v6，2026-09-08）

旧模板直接拼接持仓理由、网格理由、风险和护栏，导致同义约束反复出现；指标参与评分却未展示其影响。本次改为所有标的共用同一个依据宏：综合结论→趋势／位置与动量／量能三组证据→买入回补与部分卖出条件→信号冲突→参数依据及专属限制。旧仓与目标仓使用同等完整的指标解释。

按用户最新要求，报告不展示单位与符号说明，也不展示「共同执行规则与指标口径」折叠区；这些规则与算法口径保留在产品文档和分析逻辑中。卡片正文保留行动阶段、库存与价格、清仓参考和独立卖出回补方案；折叠区只增加该标的技术依据、具体硬过滤、原单异常和已核验清仓条件，不再逐条输出通用 `reasons/risks/strategy_guardrails/quantity_plan`。

技术依据由分析层生成，模板不计算买卖判断。缺少历史修复证据时不判定已企稳；日线样本、量比与RSI算法口径见score.md。当前数据与分析契约为v8，旧报告需要重新分析与AI复核。

## 可操作性复盘（v8）

报告定位为条件决策支持，逐卡回答当前做什么、为何、参考价格、候选份额、触发与失效条件。实际可用现金、可卖库存、在途委托及完整周期本金未核验时，不称为可直接照抄的委托表。

本次动作先于双向参数：当前退出以当前价复评，未来上涨／下跌路径另列；未持仓目标受限时明确暂停建仓。网格六项草案完整保留在卡片的“后续网格参数草案”内，数量不再与本次动作混排。确认价是峰谷恰好在阈值处的路径示例，不保证成交价。已核验部分卖出方案可展示推演剩余库存与清仓回本线，但不作为已成交账目。

旧仓按清仓复核→当前部分退出→草案触发区复核→其余等待排序。同花顺账本盈亏与实时行情各自保留口径；现金余额显示但不冒充券商实际可用现金。首页优先事项根据本批次生成，完整条件单清单包含仍监控的非网格建仓单；与暂停新增投入冲突时明确指出。原单状态不因生成报告而改变。

依据折叠区展示真实报价时间、完整日技术样本、实际过热触发依据与有效 AI 复核结论。公共单位说明和公共规则折叠区继续不展示。正式报告必须通过独立 AI 与时间闸门；质量检查通过不代表策略收益已经验证。

买入条件只列当前未满足的确认项；例如完整日样本价已高于MA5时不再提示该样本需要「站回MA5」；盘中报价另行显示，不能覆盖完整日确认，转而指出斜率、量能等缺口。不再另列全账户历史证据缺口或公共参数公式说明；卡片保留本次ATR、修正类型和标的专属条件。

验证记录：`python -X utf8 -m pytest -q` 170项通过；`compileall`、Skill `quick_validate.py` 与 `git diff --check` 通过。覆盖同ATR不同状态、超卖和初步修复、历史窗口无未来数据、零RSI、缺失与非有限值、修正后价格有效性、组合上限、库存与清仓保护、新旧契约隔离及报告去重。

本地预览 `data/reports/DEV-multi-indicator-v6-preview.html` 复用历史批次 `20260908-084148`，无新采集、无本次AI复核、未发布。15张卡片的依据区平均文本由约660字符降至348字符，同时增加综合指标解释。离线Chromium核对1440桌面及390移动视口，均无页面横向溢出；展开后的证据、参数与限制已检查。截图位于同目录 `DEV-multi-indicator-v6-preview-desktop.png`、`DEV-multi-indicator-v6-preview-mobile.png` 与 `DEV-multi-indicator-v6-preview-rationale.png`，不提交Git。测试不代表收益回测、最新账户或真实委托验证。

2026-09-08用户进一步精简：删除整段「单位：份、¥、%……」及完整公共规则折叠区，同步移除未使用的渲染参数和样式。逐卡依据、整单状态与实际执行限制保留；已刷新历史开发预览，未发布。相关76项回归测试、Skill校验与差异检查通过，1440和390视口均无横向溢出。

## 行动阶段优化（v7）

账户仓位及80%保护线只在账户总览出现一次，来源必须是已确认的账户仓位口径；市值占比回退不能冒充账户仓位。逐卡不展示「暂不启用整单：总仓……」或统一的「量价条件未确认」，改为建仓、持仓波动管理、旧仓退出等阶段和具体等待条件。无持仓目标的卖出说明为建仓后再评估，不再以「卖出库存不足」覆盖建仓需求。

买入／回补与卖出条件前移到卡片正文，依据折叠区不再重复同样条件。公共单位说明和公共规则折叠区继续隐藏。高风险分数、数据门槛、原单异常等专属依据仍保留；底仓不足、原单已停用、基准偏离和最大仓余量不足明确显示。计划标题不冒充平台操作结果，实际网格状态与全部资金约束保留。

本次验证：全量180项pytest通过，compileall、Skill quick_validate及git diff --check通过。覆盖MIXED条件候选、独立硬过滤、未持仓目标与退休旧标的区分、账户仓位只显示一次、原单停用证据与异常数量、AI一致性和旧版隔离。开发预览为data/reports/DEV-action-plans-v7-preview.html，复用历史批次20260908-084148；15张卡片中账户仓位限制出现1次、统一停用提示0次、分批建仓标题2次。离线Chromium检查1440及390视口无横向溢出，已检查已有仓位与未持仓目标卡片截图。未重新采集、未做本次AI复核、未发布，也未修改平台条件单。

## v8最终验证记录

2026-09-08重新采集真实批次`20260908-111139`并生成正式本地报告`data/reports/20260908-111139-etf-realtime.html`。最终报价时间为11:27:06～11:27:17，技术指标来自2026-09-07完整交易日。15只行情均通过精确价格、日期、量额与复权口径对账。完整日与实时确认表述经首次独立复核退回修正后，重新分析并进行新指纹复核，最终15项PASS、0项REVISE。

实际运行验证了缺AI时正式report被拒绝；附加本轮有效复核后，`scope=FORMAL_REPORT`、`formal_report_ready=true`。这表示条件报告符合契约，不表示券商资金库存已验证或委托已授权。仍监控的13条双向网格和2条建仓单全部列入冲突核对。

`python -X utf8 -m pytest -q`全量252项通过；最后补入AI报价时间字段后，相关35项再次通过。`compileall`、Skill `quick_validate.py`、`git diff --check`通过。离线Chromium检查最终HTML：15张行动卡，1440和390视口即使展开全部详情也无页面横向溢出；首页、159687、510500及移动卡片截图已检查。截图位于同一data/reports目录，不提交Git。

本次没有发布或覆盖ShareOne线上报告，没有修改真实条件单或提交委托，也没有完成收益回测。具体复盘、数据缺口与交易执行边界见[报告可操作性复盘](decision-report-audit.md)。

## v9账户资金与条件单报告

首页分别显示账户现金和80%保护线内买入额度；已知余额不再显示“未提供可用金额”。列出达到保护线所需净卖回参考金额和全部原单首轮资金压力，说明监控不等于冻结、首轮估算不是保证成交或全程最大占用。

逐卡直接显示T+0/T+1、今日累计买入与可卖量测算，卖出建议受其约束。当前退出展示价格条件单配置；等待反弹展示回落卖出初始价、负数累计回落、份额、委托价格类型和原单替换顺序。报告只使用条件单操作路径，不再要求用户改成手工下单。生成报告未修改任何原单。

## v9正式新批验证记录

真实批次`20260908-121824`已通过正式入口生成报告，分析时刻2026-09-08 12:42:58。核心采集包括13只持仓、30笔交易、46条已清仓周期及15条监控条件（13网格、2建仓）；所有时间筛选和采集首尾现金／持仓数量对账通过。15只技术样本均使用9月7日完整日线，报价时间显著列示。

附加已委托审计读取1160条历史记录，854条已成交、286条失败、13条已撤单、7条历史已申报。历史范围2025-12-08至2026-09-07，未读到更早历史终点，明确`complete=false`；不称全历史完整，不把旧状态扣作今天冻结。原始审计纳入数据指纹，变更证据会使旧AI失效，摘要只呈现状态数量和例外，完整原文留在data目录。

实际缺AI调用正式report被拒绝；本批独立复核完成后为15项PASS、0项REVISE，`scope=FORMAL_REPORT`、`formal_report_ready=true`。159687显示当前价格条件卖出计划及剩余持仓；510500的新回落卖出计划不继承旧单峰值。全部新增买入额度为0来自已计算的组合保护约束，不再称未知现金。

全量`python -X utf8 -m pytest -q`433项通过，另完成compileall、Skill quick_validate与git diff --check。最终HTML包含15张行动卡；离线Chromium检查1440和390视口，展开全部详情后均无页面横向溢出，首页及159687桌面／移动卡片截图已人工核对。报告及截图位于`data/reports/20260908-121824-*`，不提交Git。

代码、规则文档和Skill已同步；未修改真实条件单或提交任何委托。本次验证证明计算、证据与报告契约一致，没有进行策略收益回测，不能把测试或AI通过解释为盈利保证。

本批正式HTML通过ShareOne发布成功，返回链接已交付用户；发布记录与报告留在被Git忽略的data目录。该发布不代表修改券商或Touker交易状态。

## 决策复核与数量解释（v10）

报告保持账户总览、现有持仓管理、目标组合与资金承接三个主章节。账户总览集中展示新增买入门槛的含义、待确定的风险与迁移预算、最终清仓回本可能长期无法完成的限制，以及费用忽略和收益未验证的既有边界。内容读取结构化 `decision_review_v1`，不能只在模板追加风险说明而使 AI 或质量检查不可见；这些缺口不否定现有规则允许的部分卖出复评。

当前卖出候选按单笔独立展示假设回款、成交后仓位和距新增买入门槛的净卖出差额。按账本单只市值扣除拟卖份额的原价值，成交参考价与账面价之间的差额调整总资产；剩余库存保持账本估值。未来反弹计划不混入已回款或累计卖出假设，不擅自安排回款用途。

卡片正文增加减持本标的比例、可核实的账户权重与数量来源。合法原单量和约1/4持仓起拟分别说明，尚未按风险贡献优化的边界只在账户处集中解释。各卡片按当前阶段给出后续复评：未到启动价、继续下跌或长期未触发时重新检查信号与账户条件，不编造自动止损、补仓或有效期限；过热卡片明确该指标不能证明理想卖点。

价格参数折叠区区分十进制理论确认边界、首个满足比例的0.001档位示例、实际峰谷路径及委托/成交价格。主卡沿用满足档位后的参考示例，不把三位四舍五入值作为提前触发条件。公共执行处置集中展示部分成交、失败、未成交和计划重评；已委托折叠区展示已读证据支持的失败原因计数，未核验的数据不显示为零失败。

本次仅修改规则、分析和展示，不重新采集、不修改真实条件单、不覆盖正式报告或发布链接。历史批次只能用于显著标明来源和时间的 DEV 预览，旧 AI 复核不作为新契约通过证明。新增回归覆盖独立卖出情景、报价与账本估值不一致、未知输入、数量来源、后续复评约束和三章节报告；这些检查不代表策略收益验证。

### v10验证记录

2026-09-08，`python -X utf8 -m pytest -q`全量519项通过；`python -X utf8 -m compileall -q src/etfmate`、Skill `quick_validate.py`与`git diff --check`通过。新增覆盖六处卖出、五处买入确认价格及相邻档位、旧契约拒绝、决策结构篡改、AI指纹失效、单向卖单待核对与买入冲突分离、同日未来委托时刻及失败原文分类。

开发预览run-id为`DEV-V10-REPORT-AUDIT`，文件为`data/reports/DEV-v10-report-audit.html`，仅使用历史批次`20260908-121824`的原始账户、条件单和行情离线重算，不复用旧AI。分析质量为`PASS`，`formal_report_ready=false`；不生成本轮AI通过证明。预览顶部明确历史来源、未实时采集和不可作为最新实盘指令。

离线Chromium核对15张卡片，1440桌面与390移动视口在全部详情展开后均无页面横向溢出；已查看首页、策略缺口及515880手机卡片截图，核对理论边界和满足档位。预览、JSON与截图均位于被Git忽略的`data/`目录。线上旧报告未更新；最新正式报告仍需重新完整采集、分析及本轮AI复核。
