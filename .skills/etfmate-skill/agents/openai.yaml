interface:
  display_name: "ETFMate Skill"
  short_description: "采集 ETF 持仓、网格与七层证据，按组合风险预算生成一致的持仓/网格决策报告"
  default_prompt: "Use $etfmate-skill and $web-access to run a real-time ETFMate analysis from logged-in 同花顺投资账本 and Touker pages. Generate rule scores, apply portfolio risk budgets before actions, ensure HIGH risk + zero target never renders as ordinary HOLD, read ai_review_input.json, use the current host AI model for guarded AI judgement, write ai_judgements.json, and render the HTML report with compact AI status."
